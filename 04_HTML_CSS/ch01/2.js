// JavaScript source code

name = prompt("What's your name?", "Hong");
document.write(name + "~Welcome<br>");
function chk() {
    if (frm.tel.value.length < 4) {
        alert("전화번호는 뒷 4자리 이상 입력해야 해");
        return false;
    }
}

